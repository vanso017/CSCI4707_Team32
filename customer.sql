CREATE TABLE CUSTOMER(
	CustomerID INT NOT NULL,
	Fname VARCHAR(15),
	Name VARCHAR (15),
	Phone_Number INT,
	Email_Address VARCHAR(255),
	Street VARCHAR(255),
	Zip_code INT,
	City VARCHAR(15),
	State VARCHAR(15),
	PRIMARY KEY(CustomerID)
);
